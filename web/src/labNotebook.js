export const cellSource=cell=>Array.isArray(cell.source)?cell.source.join(''):String(cell.source??'');
export function newCell(type='code',source=''){return {id:crypto.randomUUID().replaceAll('-','').slice(0,12),cell_type:type,metadata:{},source,...(type==='code'?{execution_count:null,outputs:[]}: {})};}
export function parseNotebook(text){const nb=JSON.parse(text);if(nb.nbformat!==4||!Array.isArray(nb.cells))throw Error('Open a Jupyter notebook in version 4 format.');return {...nb,cells:nb.cells.map((cell,index)=>{if(!['code','markdown','raw'].includes(cell.cell_type))throw Error('Unsupported notebook cell type.');return {...cell,id:cell.id||`cell-${index}`,source:cellSource(cell),...(cell.cell_type==='code'?{outputs:Array.isArray(cell.outputs)?cell.outputs:[]}: {})}})};}
export function serializeNotebook(nb){return JSON.stringify({...nb,cells:nb.cells.map(c=>({...c,source:cellSource(c).match(/[^\n]*\n|[^\n]+$/g)||[]}))},null,2)+'\n';}
export function applyCellResult(nb,id,result,count){return {...nb,cells:nb.cells.map(c=>c.id===id?{...c,execution_count:count,outputs:result.outputs,metadata:{...c.metadata,vault:{run_id:result.id,seconds:result.seconds,status:result.status,execution:result.execution||'embedded-local'}}}:c)};}
